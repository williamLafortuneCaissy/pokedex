import { useEffect } from "react";

// pokemon = {name: '', url: ''}
const PokemonListItem = ({pokemon}) => {




    return (
        <div>
            {pokemon.name}
        </div>
    );
}

export default PokemonListItem;